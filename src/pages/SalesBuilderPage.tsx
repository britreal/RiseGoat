import { useCallback, useEffect, useMemo, useState } from 'react';
import { supabase } from '@/lib/supabase';
import { useAuth } from '@/context/AuthContext';
import { Spinner } from '@/components/ui';
import type { SalesBlock, SalesPage } from '@/types';
import {
  ArrowLeft, ChevronDown, ChevronUp, Copy, Eye, Image as ImageIcon, Loader2, Smartphone, Monitor,
  Minus, MousePointer, Palette, Plus, Save, Settings2, Trash2, Type, X,
} from 'lucide-react';

type BlockType = 'heading' | 'text' | 'image' | 'button' | 'spacer' | 'divider';
type Settings = {
  fontSize?: string; fontFamily?: string; align?: 'left' | 'center' | 'right';
  color?: string; bgColor?: string; fontWeight?: 'normal' | 'bold';
  buttonUrl?: string; buttonBg?: string; buttonColor?: string; buttonRadius?: number;
  spacerHeight?: number; imageRounded?: boolean; imageWidth?: number; paddingY?: number;
};

const fonts = ['Inter', 'Arial', 'Georgia', 'Helvetica', 'Verdana', 'Trebuchet MS', 'Times New Roman'];
const sizes = ['xs','sm','base','lg','xl','2xl','3xl','4xl','5xl','6xl'];
const sizeClass: Record<string,string> = { xs:'text-xs',sm:'text-sm',base:'text-base',lg:'text-lg',xl:'text-xl','2xl':'text-2xl','3xl':'text-3xl','4xl':'text-4xl','5xl':'text-5xl','6xl':'text-6xl' };
const blockTypes: { type: BlockType; label: string; icon: typeof Type }[] = [
  { type:'heading', label:'Título', icon:Type }, { type:'text', label:'Texto', icon:Type },
  { type:'image', label:'Imagem', icon:ImageIcon }, { type:'button', label:'Botão', icon:MousePointer },
  { type:'spacer', label:'Espaço', icon:Minus }, { type:'divider', label:'Divisor', icon:Minus },
];

function defaults(type: BlockType): { content: string; settings: Settings } {
  const map: Record<BlockType,{content:string;settings:Settings}> = {
    heading:{content:'Seu título aqui',settings:{fontSize:'3xl',fontFamily:'Inter',align:'center',color:'#0f172a',fontWeight:'bold',paddingY:8}},
    text:{content:'Escreva seu texto aqui...',settings:{fontSize:'base',fontFamily:'Inter',align:'left',color:'#334155',paddingY:8}},
    image:{content:'',settings:{align:'center',imageRounded:true,imageWidth:100,paddingY:12}},
    button:{content:'Quero começar',settings:{fontSize:'base',fontFamily:'Inter',align:'center',buttonUrl:'#',buttonBg:'#0f172a',buttonColor:'#ffffff',buttonRadius:12,paddingY:12}},
    spacer:{content:'',settings:{spacerHeight:48}}, divider:{content:'',settings:{color:'#e2e8f0',paddingY:12}},
  }; return map[type];
}

export function SalesBuilderPage({ pageId, navigate }: { pageId:string; navigate:(path:string)=>void }) {
  const { user } = useAuth();
  const [page,setPage] = useState<SalesPage|null>(null); const [blocks,setBlocks] = useState<SalesBlock[]>([]);
  const [selected,setSelected] = useState<string|null>(null); const [loading,setLoading] = useState(true); const [saving,setSaving] = useState(false); const [saved,setSaved] = useState(false); const [showSettings,setShowSettings] = useState(false); const [showAdd,setShowAdd] = useState(false);
  const selectedBlock = useMemo(()=>blocks.find(b=>b.id===selected)||null,[blocks,selected]);

  const load = useCallback(async()=>{ if(!user)return; const [p,b]=await Promise.all([supabase.from('sales_pages').select('*').eq('id',pageId).eq('user_id',user.id).maybeSingle(),supabase.from('sales_blocks').select('*').eq('page_id',pageId).eq('user_id',user.id).order('sort_order')]); setPage(p.data as SalesPage|null); setBlocks((b.data as SalesBlock[])||[]); setLoading(false); },[pageId,user]);
  useEffect(()=>{load()},[load]);

  async function persistBlock(block: SalesBlock, updates: Partial<SalesBlock>) {
    const next={...block,...updates}; setBlocks(v=>v.map(x=>x.id===block.id?next:x)); setSaving(true);
    const {error}=await supabase.from('sales_blocks').update({content:next.content,settings:next.settings,sort_order:next.sort_order}).eq('id',block.id).eq('user_id',user?.id||'');
    setSaving(false); if(!error){setSaved(true);setTimeout(()=>setSaved(false),1200)};
  }
  async function add(type:BlockType){ if(!user||!page)return; const d=defaults(type); const {data,error}=await supabase.from('sales_blocks').insert({page_id:page.id,user_id:user.id,block_type:type,content:d.content,settings:d.settings,sort_order:blocks.length}).select().single(); if(!error&&data){setBlocks(v=>[...v,data as SalesBlock]);setSelected((data as SalesBlock).id)} setShowAdd(false); }
  async function remove(id:string){ await supabase.from('sales_blocks').delete().eq('id',id).eq('user_id',user?.id||''); const next=blocks.filter(b=>b.id!==id).map((b,i)=>({...b,sort_order:i})); setBlocks(next); for(const b of next) await supabase.from('sales_blocks').update({sort_order:b.sort_order}).eq('id',b.id).eq('user_id',user?.id||''); setSelected(null); }
  async function move(i:number,dir:-1|1){const j=i+dir;if(j<0||j>=blocks.length)return;const next=[...blocks];[next[i],next[j]]=[next[j],next[i]];setBlocks(next);for(let n=0;n<next.length;n++)await supabase.from('sales_blocks').update({sort_order:n}).eq('id',next[n].id).eq('user_id',user?.id||'');}
  async function duplicate(block:SalesBlock){if(!user||!page)return;const d=await supabase.from('sales_blocks').insert({page_id:page.id,user_id:user.id,block_type:block.block_type,content:block.content,settings:block.settings,sort_order:block.sort_order+1}).select().single();if(d.data){const shifted=blocks.map(b=>b.sort_order>=block.sort_order+1?{...b,sort_order:b.sort_order+1}:b);for(const b of shifted)await supabase.from('sales_blocks').update({sort_order:b.sort_order}).eq('id',b.id).eq('user_id',user.id);setBlocks([...shifted,d.data as SalesBlock].sort((a,b)=>a.sort_order-b.sort_order));}}
  async function togglePublish(){if(!page)return;const {error}=await supabase.from('sales_pages').update({is_published:!page.is_published,updated_at:new Date().toISOString()}).eq('id',page.id).eq('user_id',user?.id||'');if(!error)setPage({...page,is_published:!page.is_published});}
  async function savePage(){if(!page)return;setSaving(true);await supabase.from('sales_pages').update({seo_title:page.seo_title,seo_description:page.seo_description,updated_at:new Date().toISOString()}).eq('id',page.id).eq('user_id',user?.id||'');setSaving(false);setSaved(true);setTimeout(()=>setSaved(false),1200)}
  if(loading)return <Spinner/>;

  return <div className="min-h-screen bg-slate-100 flex flex-col">
    <header className="sticky top-0 z-40 h-14 bg-white border-b border-slate-200 flex items-center justify-between px-3 lg:px-5">
      <div className="flex items-center gap-3 min-w-0"><button onClick={()=>navigate('/sales')} className="p-2 rounded-lg hover:bg-slate-100"><ArrowLeft className="w-4 h-4"/></button><div className="min-w-0"><p className="font-semibold text-sm truncate">{page?.title}</p><p className="text-[11px] text-slate-400">risegoat.com/p/{page?.slug}</p></div></div>
      <div className="flex items-center gap-2"><button onClick={()=>setShowSettings(v=>!v)} className="p-2 rounded-lg hover:bg-slate-100" title="SEO"><Settings2 className="w-4 h-4"/></button>{saved&&<span className="text-xs text-green-600">Salvo</span>}<button onClick={savePage} disabled={saving} className="px-3 py-1.5 rounded-lg bg-slate-900 text-white text-xs font-medium">{saving?<Loader2 className="w-3.5 h-3.5 animate-spin"/>:<Save className="w-3.5 h-3.5 inline mr-1"/>}Salvar</button><button onClick={togglePublish} className={`px-3 py-1.5 rounded-lg text-xs font-medium ${page?.is_published?'bg-amber-50 text-amber-700':'bg-green-50 text-green-700'}`}>{page?.is_published?'Despublicar':'Publicar'}</button></div>
    </header>
    {showSettings&&<div className="bg-white border-b border-slate-200 p-4 grid md:grid-cols-2 gap-3"><input value={page?.seo_title||''} onChange={e=>setPage(p=>p?{...p,seo_title:e.target.value}:p)} placeholder="Título SEO" className="px-3 py-2 border rounded-lg text-sm"/><input value={page?.seo_description||''} onChange={e=>setPage(p=>p?{...p,seo_description:e.target.value}:p)} placeholder="Descrição SEO" className="px-3 py-2 border rounded-lg text-sm"/></div>}
    <div className="flex flex-1 min-h-0">
      <aside className="hidden xl:block w-56 bg-white border-r border-slate-200 p-4 sticky top-14 h-[calc(100vh-3.5rem)] self-start overflow-y-auto"><p className="text-[11px] font-semibold uppercase tracking-wider text-slate-400 mb-3">Blocos</p><div className="grid grid-cols-2 gap-2">{blockTypes.map(b=><button key={b.type} onClick={()=>add(b.type)} className="p-3 rounded-xl border border-slate-200 hover:border-cyan-400 hover:bg-cyan-50 text-xs flex flex-col items-center gap-1.5"><b.icon className="w-4 h-4"/><span>{b.label}</span></button>)}</div></aside>
      <main className="flex-1 overflow-y-auto p-3 lg:p-8"><div className="max-w-5xl mx-auto">
        <div className="flex items-center justify-between mb-4 bg-white rounded-xl border border-slate-200 p-1.5 shadow-sm">
          <p className="px-2 text-xs font-semibold text-slate-600">Visualização</p>
          <div className="flex items-center gap-1">
            <button onClick={()=>setPreviewMode('iphone')} className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium transition ${previewMode==='iphone'?'bg-slate-900 text-white':'text-slate-500 hover:bg-slate-100'}`}><Smartphone className="w-3.5 h-3.5"/> iPhone</button>
            <button onClick={()=>setPreviewMode('desktop')} className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium transition ${previewMode==='desktop'?'bg-slate-900 text-white':'text-slate-500 hover:bg-slate-100'}`}><Monitor className="w-3.5 h-3.5"/> Desktop</button>
          </div>
        </div>
        <div className={previewMode==='iphone' ? 'max-w-[390px] mx-auto border-[10px] border-slate-900 rounded-[2.25rem] shadow-xl overflow-hidden' : 'w-full'}>
        <div className="bg-white min-h-[700px] shadow-sm overflow-hidden" onClick={()=>setSelected(null)}>{blocks.length===0?<div className="min-h-[700px] flex items-center justify-center text-slate-400">Adicione seu primeiro bloco</div>:blocks.map((b,i)=><div key={b.id} onClick={e=>{e.stopPropagation();setSelected(b.id)}} className={`relative ${selected===b.id?'ring-2 ring-cyan-400 ring-inset':''}`}><BlockPreview block={b}/>{selected===b.id&&<div className="absolute right-2 top-2 flex gap-1 bg-white shadow rounded-lg p-1"><button onClick={()=>move(i,-1)} disabled={i===0} className="p-1 disabled:opacity-30"><ChevronUp className="w-3.5 h-3.5"/></button><button onClick={()=>move(i,1)} disabled={i===blocks.length-1} className="p-1 disabled:opacity-30"><ChevronDown className="w-3.5 h-3.5"/></button><button onClick={()=>duplicate(b)} className="p-1"><Copy className="w-3.5 h-3.5"/></button><button onClick={()=>remove(b.id)} className="p-1 text-red-500"><Trash2 className="w-3.5 h-3.5"/></button></div>}</div>)}</div>
        <button onClick={()=>setShowAdd(v=>!v)} className="w-full mt-3 py-3 border-2 border-dashed border-slate-300 rounded-xl bg-white text-slate-500 hover:border-cyan-400 hover:text-cyan-600 text-sm flex justify-center gap-2"><Plus className="w-4 h-4"/>Adicionar bloco</button>
        {showAdd&&<div className="xl:hidden mt-2 p-3 bg-white rounded-xl border grid grid-cols-3 gap-2">{blockTypes.map(b=><button key={b.type} onClick={()=>add(b.type)} className="p-3 border rounded-lg text-xs"><b.icon className="w-4 h-4 mx-auto mb-1"/>{b.label}</button>)}</div>}
      </div></main>
      <aside className="hidden lg:block w-72 bg-white border-l border-slate-200 p-4 sticky top-14 h-[calc(100vh-3.5rem)] self-start overflow-y-auto">{selectedBlock?<Inspector block={selectedBlock} onUpdate={u=>persistBlock(selectedBlock,u)}/>:<div className="text-center text-slate-400 text-xs pt-16"><Palette className="w-7 h-7 mx-auto mb-2"/>Selecione um bloco para editar</div>}</aside>
    </div>
  </div>;
}

function BlockPreview({block}:{block:SalesBlock}){const s=block.settings as Settings;const align=s.align||'left';const common={fontFamily:s.fontFamily||'Inter',color:s.color||'#0f172a',backgroundColor:s.bgColor||'transparent',paddingTop:s.paddingY||8,paddingBottom:s.paddingY||8};const cls=sizeClass[s.fontSize||'base']||'text-base';const wrap=align==='center'?'text-center':align==='right'?'text-right':'text-left';if(block.block_type==='heading'||block.block_type==='text')return <div className={`${wrap} px-6`} style={common}><p className={`${cls} leading-relaxed ${s.fontWeight==='bold'?'font-bold':''} whitespace-pre-wrap`}>{block.content}</p></div>;if(block.block_type==='image')return <div className={`px-6 flex ${align==='center'?'justify-center':align==='right'?'justify-end':'justify-start'}`} style={{paddingTop:s.paddingY||12,paddingBottom:s.paddingY||12,backgroundColor:s.bgColor||'transparent'}}>{block.content?<img src={block.content} alt="" style={{width:`${s.imageWidth||100}%`}} className={`max-w-full object-contain ${s.imageRounded===false?'':'rounded-xl'}`}/>:<div className="h-40 w-full bg-slate-100 rounded-xl flex items-center justify-center text-slate-400"><ImageIcon/></div>}</div>;if(block.block_type==='button')return <div className={`px-6 flex ${align==='center'?'justify-center':align==='right'?'justify-end':'justify-start'}`} style={{paddingTop:s.paddingY||12,paddingBottom:s.paddingY||12,backgroundColor:s.bgColor||'transparent'}}><span className="inline-block px-7 py-3 font-semibold" style={{fontFamily:s.fontFamily||'Inter',fontSize:'15px',borderRadius:s.buttonRadius??12,backgroundColor:s.buttonBg||'#0f172a',color:s.buttonColor||'#fff'}}>{block.content}</span></div>;if(block.block_type==='spacer')return <div style={{height:s.spacerHeight||48,backgroundColor:s.bgColor||'transparent'}}/>;return <hr className="border-t" style={{marginTop:s.paddingY||12,marginBottom:s.paddingY||12,borderColor:s.color||'#e2e8f0'}}/>}

function Inspector({block,onUpdate}:{block:SalesBlock;onUpdate:(u:Partial<SalesBlock>)=>void}){const s=block.settings as Settings;const set=(x:Partial<Settings>)=>onUpdate({settings:{...s,...x}});const [content,setContent]=useState(block.content);useEffect(()=>setContent(block.content),[block.id,block.content]);const commit=()=>{if(content!==block.content)onUpdate({content})};return <div className="space-y-4"><div className="flex items-center justify-between"><p className="font-semibold text-sm">Editar {block.block_type}</p><span className="text-[10px] text-slate-400">RiseGoat</span></div>{(block.block_type==='heading'||block.block_type==='text'||block.block_type==='button')&&<textarea value={content} onChange={e=>setContent(e.target.value)} onBlur={commit} rows={block.block_type==='heading'?2:4} className="w-full border rounded-lg p-2 text-sm" placeholder="Conteúdo"/>}{block.block_type==='image'&&<input value={content} onChange={e=>setContent(e.target.value)} onBlur={commit} placeholder="URL da imagem" className="w-full border rounded-lg p-2 text-sm"/>}<div className="grid grid-cols-2 gap-2"><label className="text-xs text-slate-500">Fonte<select value={s.fontFamily||'Inter'} onChange={e=>set({fontFamily:e.target.value})} className="w-full mt-1 border rounded-lg p-1.5 bg-white">{fonts.map(f=><option key={f}>{f}</option>)}</select></label><label className="text-xs text-slate-500">Tamanho<select value={s.fontSize||'base'} onChange={e=>set({fontSize:e.target.value})} className="w-full mt-1 border rounded-lg p-1.5 bg-white">{sizes.map(x=><option key={x}>{x}</option>)}</select></label></div>{(block.block_type==='heading'||block.block_type==='text'||block.block_type==='button'||block.block_type==='image')&&<><div><p className="text-xs text-slate-500 mb-1">Alinhamento</p><div className="grid grid-cols-3 gap-1">{(['left','center','right'] as const).map(a=><button key={a} onClick={()=>set({align:a})} className={`p-1.5 rounded border text-xs ${s.align===a?'bg-cyan-50 border-cyan-400':''}`}>{a==='left'?'Esq.':a==='center'?'Centro':'Dir.'}</button>)}</div></div><div className="grid grid-cols-2 gap-3"><label className="text-xs text-slate-500">Texto<input type="color" value={s.color||'#0f172a'} onChange={e=>set({color:e.target.value})} className="block w-full h-8"/></label><label className="text-xs text-slate-500">Fundo<input type="color" value={s.bgColor||'#ffffff'} onChange={e=>set({bgColor:e.target.value})} className="block w-full h-8"/></label></div></>}{block.block_type==='heading'&&<label className="flex gap-2 text-xs"><input type="checkbox" checked={s.fontWeight==='bold'} onChange={e=>set({fontWeight:e.target.checked?'bold':'normal'})}/> Negrito</label>}{block.block_type==='button'&&<><input value={s.buttonUrl||'#'} onChange={e=>set({buttonUrl:e.target.value})} placeholder="URL do botão" className="w-full border rounded-lg p-2 text-sm"/><div className="grid grid-cols-2 gap-3"><label className="text-xs text-slate-500">Botão<input type="color" value={s.buttonBg||'#0f172a'} onChange={e=>set({buttonBg:e.target.value})} className="block w-full h-8"/></label><label className="text-xs text-slate-500">Texto<input type="color" value={s.buttonColor||'#ffffff'} onChange={e=>set({buttonColor:e.target.value})} className="block w-full h-8"/></label></div><label className="text-xs text-slate-500">Arredondamento<input type="range" min="0" max="40" value={s.buttonRadius??12} onChange={e=>set({buttonRadius:Number(e.target.value)})} className="w-full"/></label></>}{block.block_type==='image'&&<><label className="text-xs text-slate-500">Largura: {s.imageWidth??100}%<input type="range" min="20" max="100" value={s.imageWidth??100} onChange={e=>set({imageWidth:Number(e.target.value)})} className="w-full"/></label><label className="flex gap-2 text-xs"><input type="checkbox" checked={s.imageRounded!==false} onChange={e=>set({imageRounded:e.target.checked})}/> Cantos arredondados</label></>}{block.block_type==='spacer'&&<label className="text-xs text-slate-500">Altura: {s.spacerHeight||48}px<input type="range" min="8" max="240" value={s.spacerHeight||48} onChange={e=>set({spacerHeight:Number(e.target.value)})} className="w-full"/></label>}<label className="text-xs text-slate-500">Espaço vertical: {s.paddingY||8}px<input type="range" min="0" max="64" value={s.paddingY||8} onChange={e=>set({paddingY:Number(e.target.value)})} className="w-full"/></label></div>}
